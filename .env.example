# Lead delivery webhook (server-side only — never exposed to the browser).
# Create an "Inbound Webhook" trigger in a GoHighLevel workflow and paste
# its URL here. Set this in Vercel: Project → Settings → Environment
# Variables (Production + Preview). For local dev, copy to .env.local.
LEAD_WEBHOOK_URL=
